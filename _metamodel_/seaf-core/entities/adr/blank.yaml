# Бланк архитектурного решения
entities:
  seaf.adr:
    presentations:
      blank:
        title: Карточка требования
        params:
          type: object
          properties:
            seaf-adr-id:
              title: Идентификатор требования
              type: string
          required:
            - seaf-adr-id
        type: markdown
        template: templates/blank.md
        source: >
          (
              $id := $params."seaf-adr-id";
              $requirements := $."seaf.requirements";
              $adr := $."seaf.adr";
              $data := $lookup($."seaf.adr", $id);
              $merge([$data, {
                  "id": $id,
                  "requirements": $data.requirements.(
                      $title := $lookup($requirements, $).title;
                      {
                          "title": $title ? $title : $,
                          "link": "/entities/seaf.requirements/blank?seaf-req-id=" & $
                      } 
                  ),
                  "dependencies": $data.dependencies.(
                      $title := $lookup($adr, $).title;
                      {
                          "title": $title ? $title : $,
                          "link": "/entities/seaf.adr/blank?seaf-adr-id=" & $
                      } 
                  ),
                  "blocked": $adr.$spread().(
                      $depId := $keys()[0];
                      *.dependencies[$=$id] ? (
                        $title := $lookup($adr, $depId).title;
                        {
                            "title": $title ? $title : $,
                            "link": "/entities/seaf.adr/blank?seaf-adr-id=" & $depId
                        } 
                      ) 
                  )
              }])
          )
