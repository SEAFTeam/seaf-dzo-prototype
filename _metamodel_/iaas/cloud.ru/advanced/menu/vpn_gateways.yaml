entities:
  seaf.ta.reverse.cloud_ru.advanced.vpn_gateways:
    menu: >
      (
          $count($$."seaf.ta.reverse.cloud_ru.advanced.vpn_gateways") > 0 ? [{
                      /* Размещение в пользовательском меню */
                      "location": entities."seaf.ta.reverse".config.main_menu & "/Cloud.ru/Advanced/Реестры/VPN Gateways",
                      /* URI представления context см. ниже.*/
                      "link": "entities/seaf.ta.reverse.cloud_ru.advanced.vpn_gateways/table_view"
          }];
      )